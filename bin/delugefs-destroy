#!/bin/sh

ZFS_DATASET="scratch/dfs"
CLUSTER_USER="root"
USER_ID="0"
CLUSTER_ID="${1}"
shift
if [ "x" != "x${1}" ]; then
    ZFS_DATASET="${1}"
    shift
fi

umount -f /dgfs/${CLUSTER_ID}
umount -f /${ZFS_DATASET}/_delugefs_${CLUSTER_ID}/tmp/

pkill gtfcwatch
pkill ${ZFS_DATASET}
pkill ${ZFS_DATASET}2

zfs destroy -r ${ZFS_DATASET}
zfs destroy -r ${ZFS_DATASET}2
